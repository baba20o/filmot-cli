[build-system]
requires = ["setuptools>=77.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "filmot-cli"
dynamic = ["version"]
description = "A powerful CLI for searching YouTube transcripts via the Filmot API"
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
authors = [
    {name = "baba20o", email = "baba200@greenmountaincomputing.com"}
]
keywords = ["youtube", "transcripts", "subtitles", "filmot", "cli", "search"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Utilities",
]
dependencies = [
    "requests>=2.28.0",
    "python-dotenv>=1.0.0",
    "click>=8.0.0",
    "rich>=13.0.0",
    "youtube-transcript-api>=1.1.0",
    "google-api-python-client>=2.100.0",
]

[project.optional-dependencies]
aws = [
    "boto3>=1.26.0",
    "yt-dlp>=2023.1.0",
]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
]

[project.urls]
Homepage = "https://github.com/baba20o/filmot-cli"
Repository = "https://github.com/baba20o/filmot-cli"
Issues = "https://github.com/baba20o/filmot-cli/issues"

[project.scripts]
filmot = "filmot.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["filmot*"]

[tool.setuptools.dynamic]
version = {attr = "filmot._version.__version__"}
